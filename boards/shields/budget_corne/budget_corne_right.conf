# Right half (peripheral) — keyboard only on ZMK v0.3.0
# PAT912x (CONFIG_INPUT_PAT912X) needs ZMK main / Zephyr 4.x — not available here yet.

CONFIG_BT=y
CONFIG_ZMK_BLE=y
CONFIG_ZMK_USB=n

CONFIG_ZMK_DISPLAY=n
CONFIG_ZMK_POINTING=n
